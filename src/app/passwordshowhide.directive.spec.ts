import { PasswordshowhideDirective } from './passwordshowhide.directive';

describe('PasswordshowhideDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordshowhideDirective();
    expect(directive).toBeTruthy();
  });
});
